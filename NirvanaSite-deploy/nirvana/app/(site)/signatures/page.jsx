export const metadata = {
  title: 'Signatures | Nirvana',
  description: 'Signature previews for Nirvana Luxe',
};

import SignaturePreview from '../../../src/signatures/SignaturePreview';

export default function SignaturesPage() {
  return <SignaturePreview />;
}
